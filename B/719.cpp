#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main(){
	int n;
	cin>>n;
	vector<char> v(n);
	vector<int> c(n);
	for (int i=0;i<n;i++){
		cin>>v[i];
		if (v[i]=='r')v[i]=1;
		if (v[i]=='b')v[i]=0;
	}
	for (int i=0;i<dsfa;i++){
			
		}
	}

}
